First off, import antigravity indeed! You are set up perfectly to fly through this. Your first prompt is an absolute masterclass in hackathon scoping. You've established the persona, locked in the constraints, and set a strict boundary so the LLM doesn't hallucinate a bunch of code you aren't ready for.

---

## 🚀 Here is your exact battle plan from this moment forward

### **Step 1: Lock in Phase 1 and Validate**

Once your AI generates `agent.yaml`, `SOUL.md`, and `RULES.md`, do not just glance at them.

- Create the repository structure exactly as specified.
- Paste the generated markdown and YAML into their respective files.

Open your terminal in the root of that repo and run the validation commands provided by the hackathon:

```bash
npx gitagent validate
npx gitagent info

If it throws an error, fix the YAML spacing or missing headers immediately before moving on.

Step 2: Feed the Phase 2 Prompt (repo-autopsy)

Once Phase 1 is validated, you need to build your anchor skill.

Since gitclaw supports Python and system binaries, we can leverage Python-based static analysis tools like bandit or pylint via the run_command tool to do the heavy lifting for the autopsy.

Copy and paste this prompt to your AI:

Prompt 2: Phase 2 Execution
Perfect. The identity is locked. Let's move to Phase 2: Skill 1 - The Past (repo-autopsy).

I need you to write the exact contents of skills/repo-autopsy/SKILL.md.

Requirements:
- Frontmatter: Must include name: repo-autopsy, a cynical but accurate description, and allowed-tools: read_file list_directory run_command write_file.
- Instructions: Write strict markdown instructions telling the agent how to execute this skill. It must:
  - Use run_command to execute Python static analysis (like bandit or pylint) or basic grep searches for common vulnerabilities and hardcoded secrets.
  - Analyze the output with the cynical eye of a forensic analyst.
  - Use write_file to output autopsy-report.md.
  - The report MUST cite exact file paths and line numbers, and rate the severity of each finding.

Output ONLY the contents of SKILL.md including the YAML frontmatter. Do not write any other files.
Step 3: Feed the Phase 3 Prompt (mortem-interrogator)

Once repo-autopsy is saved, we build the "wow factor" interaction.

Prompt 3: Phase 3 Execution
Awesome. Now for Phase 3: Skill 2 - The Present (mortem-interrogator).

I need you to write the exact contents of skills/mortem-interrogator/SKILL.md.

Requirements:
- Frontmatter: name: mortem-interrogator, description, and allowed-tools: read_file write_file.
- Instructions:
  - The agent must be instructed to read an incident.md file (which we will provide during the demo).
  - It must internally run a "Five Whys" loop based on the incident text.

CRITICAL RULE:
- It must explicitly reject "human error", "developer typo", or "we didn't know" as root causes.
- It must trace the failure back to a systemic or institutional flaw (e.g., lack of automated testing, poor code review culture, broken CI/CD pipeline).

- It must output its final verdict to systemic-finding.md.

Output ONLY the contents of SKILL.md.
Step 4: Feed the Phase 4 Prompt (The merge-risk Stub)

Keep this one lightweight so you don't burn demo time debugging it.

Prompt 4: Phase 4 Execution
Finally, Phase 4: Skill 3 - The Future (merge-risk).

We are stubbing this out for the demo to show the architectural vision. Write the contents of skills/merge-risk/SKILL.md.

Requirements:
- Frontmatter: name: merge-risk, description, allowed-tools: read_file write_file.
- Instructions:
  - Keep the logic thin but the persona strong.
  - Instruct the agent to read a diff.txt file representing an upcoming merge.
  - It should map the changed files to the systemic findings from mortem-interrogator.
  - Write a merge-risk.md report warning the team if they are about to repeat history.

Output ONLY the contents of SKILL.md.
Step 5: Test the Runtime

Once all files are in place, you are ready to bring CausalLoop to life.

Install the SDK:

npm install gitclaw

Then, start feeding it test files:

A dummy repo with a deliberate flaw
A fake incident.md

Observe how the agent reacts.

🎯 Stay focused on getting that 2-minute demo script working flawlessly.